import React from "react"
import styled from "styled-components"
import SectionLayout from "../../layouts/section"
import { sectionIds } from "../../config"

const Work = props => (
  <SectionLayout title={sectionIds.WORK} id={sectionIds.WORK}>
    just a child
  </SectionLayout>
)

export default Work
